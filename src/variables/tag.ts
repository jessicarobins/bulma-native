import { misc } from './base';
import { textColors, generalColors } from './derived';

const tag = {
  tagBackgroundColor: generalColors.background,
  tagColor: textColors.text,
  tagRadius: misc.radius,
  tagDeleteMargin: 1,
};

export default tag;
