import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  backdrop: {
    position: 'absolute',
    width: 10000,
    height: 10000,
    left: -5000,
    top: -5000,
    right: 0,
    bottom: 0,
  },
});

export default styles;
